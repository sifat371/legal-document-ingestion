# legal-document-ingestion
This project explores different approaches to ingesting legal PDFs for Retrieval-Augmented Generation (RAG) systems. It compares a custom extraction pipeline with a Docling-based structured parsing approach.
